name: devbox_flutter
description: "DevBox — Control your AI from your phone"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  web_socket_channel: ^3.0.2
  http: ^1.3.0
  mobile_scanner: ^7.0.1
  provider: ^6.1.2
  gpt_markdown: ^1.0.0
  google_fonts: ^6.0.0
  flutter_animate: ^4.0.0
  flutter_local_notifications: ^18.0.0
  shared_preferences: ^2.5.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
